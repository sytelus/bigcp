# ADR 0001: Use Rust

**Status:** Accepted

## Context

The tool requires a narrow unsafe Win32 boundary, explicit ownership, and high-throughput native code.

## Decision

Use stable Rust 2024 with MSVC; deny unsafe outside bigcp-win.

## Consequences

Memory safety and strong result types are default; the pinned toolchain and Windows build tools are required.
