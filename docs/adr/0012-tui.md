# ADR 0012: Ratatui terminal UX

**Status:** Accepted

## Context

The live and saved views need rich navigation without coupling UI to workers.

## Decision

Use ratatui/crossterm over immutable snapshots, with plain fallback.

## Consequences

Terminal failure cannot mutate copy state; redirected output stays script-friendly.
